# The @refino/dsh-plugin bundle patch: one row mounting the refino plugin.
# Installed into a profile via `dsh plugin --profile <name> add @refino/dsh-plugin`.
- insert:
    - id: refino
      name: "@refino/dsh-plugin"
