# The @refino/cordis-plugin-refino bundle patch: one row mounting the refino plugin.
# Installed into a profile via `dsh plugin --profile <name> add @refino/cordis-plugin-refino`.
- insert:
    - id: refino
      name: "@refino/cordis-plugin-refino"
