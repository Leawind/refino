# @refino/node

`refino` 引擎的 Node.js 存储适配器，是 `.refino/` 目录存储格式的唯一读写入口。

## 职责边界

本包提供：

- 加载：读取 `.refino/` 目录并构建内存图（解析委托给 `refino` 引擎）
- 创建：写入前提节点与约束节点文件（序列化与 ID 生成委托给 `refino` 引擎）

本包不提供：

- 解析、校验、查询等图逻辑（由 `refino` 引擎提供）
- 命令行接口、可视化界面
