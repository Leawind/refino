# @refino/storage

CRG 文件系统存储格式的权威定义与实现，也是 `.refino/` 目录的唯一读写入口。

## 职责边界

本包提供：

- 加载：读取 `.refino/` 目录并构建内存图（图逻辑委托给 `refino` 引擎）
- 单节点读取：按 ID 直接读取并解析单个节点文件（候选路径的取舍与重复检出规则与全量加载一致）
- 创建、更新、删除：写入前提节点与约束节点文件（ID 生成委托给 `refino` 引擎）
- 原子写：所有写入操作采用临时文件 + rename，保证监听与并发读取不会撞到半写文件
- 存储格式的定义：目录结构、节点文件格式、Markdown/YAML 解析、序列化与摘要提取规则

本包不提供：

- 图数据模型与图逻辑：校验、查询、写入前 grounds 校验等（由 `refino` 引擎提供）
- 命令行接口、可视化界面

## 存储布局

```
.refino/
└── nodes/
    └── <分片>/
        └── <文件名>.<类型>.md
```

- 所有节点放入单一顶层目录 `nodes/`，不区分前提与约束子目录。
- 分片目录名为节点 ID 的前 2 个字符；文件名为节点 ID 的后 6 个字符加上类型后缀：`<后 6 字符>.<type>.md`，`<type>` 取 `premise` 或 `constraint`。各段均为 Crockford base32 字符（数字与 A-Z，不含 I、L、O、U），分片与文件名前段合起来构成 8 字符的节点 ID。

## ID 推导规则（路径即身份）

节点 ID 完全由文件路径推导：`ID = 分片目录名 + 文件名去掉 .md 与类型后缀`；节点类型同样来自文件名的类型段。因此不存在"ID 与分片不符"或"类型不一致"的情况；加载时校验的是路径形状本身——非法分片目录与非 Markdown 文件被静默忽略，文件名缺少合法的 `<6 字符 ID>.<类型>` 形状报 `INVALID_NODE_PATH`，ID 段不足 6 个合法字符报 `INVALID_ID`。同一 ID 的全部候选位置恰为两条路径（`.premise.md` 与 `.constraint.md`），ID 全局唯一在写入时即可通过这两条路径检出。

## 节点文件格式

每个节点文件是可选 YAML frontmatter 加 Markdown 正文：

- frontmatter 字段：约束节点可用 `grounds`（依据 ID 列表）与 `rationale`（理由）；前提节点可用 `confirmed`（RFC 3339 带显式 UTC 偏移的确认时间）；两类节点均可用 `summary`（独立摘要属性）。
- ID 与类型都不出现在文件内容中（类型由文件名表达）。
- frontmatter 中的未知字段一律忽略，不作错误处理。

## 摘要规则

摘要是独立于正文的属性（见 crg.md）：frontmatter 声明 `summary` 字段时直接采用；未声明时回退为正文首段，折叠内部空白为单行，超过 100 字符时截断并追加 `...`。截断仅发生在回退路径上，显式 `summary` 字段与正文本身不限制字数。
